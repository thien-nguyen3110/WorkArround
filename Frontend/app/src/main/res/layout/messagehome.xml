<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".messageActivity"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:background="#FFF0E0"
    android:orientation="vertical">

    <!-- Toolbar for Back Button -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:background="#FFA500"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
    </androidx.appcompat.widget.Toolbar>

    <!-- Individual Messages Divider -->
    <TextView
        android:id="@+id/individual_messages_divider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Individual Messages"
        android:textSize="18sp"
        android:textStyle="bold">
    </TextView>

    <!-- RecyclerView for Individual Messages -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_individual"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical">
    </androidx.recyclerview.widget.RecyclerView>

    <!-- Group Messages Divider -->
    <TextView
        android:id="@+id/group_messages_divider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Group Messages"
        android:textSize="18sp"
        android:textStyle="bold">
    </TextView>

    <!-- RecyclerView for Group Messages -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_group"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical">
    </androidx.recyclerview.widget.RecyclerView>

</androidx.appcompat.widget.LinearLayoutCompat>
